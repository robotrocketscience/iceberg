# R-leo-depot-bootstrap — STUDY

**Round:** R-leo-depot-bootstrap. SCOPE pre-registered 2026-07-20, scripted bounds.
**Worker:** worktree-115637 session. Kills the follow-on R174 proposed for itself.

## Results

All four hypotheses **HELD** — the arc's first clean sweep.

| # | Claim | Measured |
| --- | --- | --- |
| H1 | water interest rate 4.3–18.9 t consumed per t delivered | 4.3 (80 t chunk, 480 s kick) to 18.9 (canonical, lit spiral) |
| H2 | break-even outbound ≤ 4,867 m/s; no real mode reaches it | best break-even 4,867 m/s vs cheapest real outbound 7,300 m/s |
| H3 | fleet ledger diverges ≥ 3.3 t drained per delivered tonne | best corner drains 3.3 t per delivered tonne; a 500 t seed depot survives at most 2 missions |
| H4 | ops-only salvage rate 0.15–0.18 (accretive) | 0.15–0.18 |

**Reading.** "The product as its own propellant" fails arithmetic that a single line of the rocket equation exposes: every depot-fueled outbound mode consumes 4–19 times the water it delivers, because the outbound delta-v (≥ 7.3 km/s) sits far above the break-even (≤ 4.9 km/s even for the biggest chunk at 800 s). The bootstrap is a pyramid that eats its own base. What survives is the modest, real version: depot water for low-delta-v service — departure ops, arrival trim, customer delivery shuttling — at a 0.15–0.18 interest rate, which is accretive and already implicit in the conops' depot-side operations.

## Revisit (mandatory)

Honest note on the clean sweep: H1 and H4's bands were taken directly from the pre-script's own outputs, so their "held" verdicts verify implementation consistency more than prediction — the falsifiable content of this round lives in H2 and H3's structural claims (break-even unreachable; ledger diverges), which are genuine and held. The convention needs this distinction stated: **script-derived bounds test the model's stability; structural hypotheses test the world.** Both belong in a SCOPE, labeled as what they are. Thin spots: hardware masses inherited from R174 corners; no depot boil-off or delivery losses (both worsen the verdict, so the kill is conservative); the reactor-fleet depot question is out of scope pending mission-graph internals.

## Cross-learning

- **Kill:** the depot-bootstrap economics rescue, before it entered any pitch or matrix cell. The economics story must stand on R174's 4.8× (non-fission) or the reactor baseline — not on circular water.
- **Adopt (scoped):** depot water for ≤ 1 km/s service legs, rate 0.15–0.18.
- **For the orchestrator:** R173 + R174 + R175 travel together as one arc for any matrix amendment: inbound floor 1.74× → end-to-end 4.8× → no bootstrap escape. Bet #3 final form stands at R174's.
- **Follow-ons:** kick staging optimization (still the 81 percent line item); reactor-fleet depot ledger once mission-graph internals price the baseline outbound water.
