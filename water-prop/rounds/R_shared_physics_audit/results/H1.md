# H1 — Convention-incorrect round count

**Hypothesis (SCOPE):** 3 ≤ N ≤ 12 convention-incorrect rounds out of the 146-directory tree.

**Mimas point prediction:** N ≈ 5–7.

**Observed:** N = 3 convention-incorrect rounds.

**Verdict:** **HELD at the low edge of the SCOPE band; mimas point prediction overshot.**

The three convention-incorrect rounds are:

1. `R_electric_outbound` (run.py:223, run.py:230) — hyperion's original catch. Local `constant_thrust_burn` is wet-convention; `m_tug_t` is post-burn dry mass. (Already superseded by `R_electric_outbound_rerun`.)
2. `R_megawatt_architecture_viability` (run.py:76) — same bug class. Local `constant_thrust_burn` is wet-convention; `m_dry_t` is post-burn dry mass.
3. `R_redundancy_budget_cost` (run.py:226) — same bug class on the outbound burn in the all-electric branch. Local `constant_thrust_burn` is wet-convention; `m_dry` is post-burn dry mass. (Sibling call at line 222 inside the `inbound_chunkfed=True` branch passes `m_after_kick`, which IS the wet mass at start of the next electric burn — that call is correct; the variable name tripped a heuristic false-positive that mimas resolved manually.)

**Excluded by manual carve-out (deliberate or false positive):**

- `R_electric_outbound_rerun` lines 195, 225, 234: three call sites that the AST walker flagged as convention-incorrect. All three are intentional bug-pattern reproductions in the rerun round itself, demonstrating that the rerun caught the hyperion bug. Documented in the source. Not contamination.

**Ambiguous rounds (verdict deferred):**

- `R6_power_constrained_isp_optimum` — calls shared library `power_optimal_isp`. The library function has a `m_initial_kg` parameter that mimas reads as wet-at-start by inspecting the math (`m_p = m_initial_kg * (1 - exp(-delta_v / v_e))`), but the function has no entry assertion and the round's call sites pass `m_initial_kg=tug_dry_t + chunk_t + propellant_estimate`, which is a self-consistent wet-mass-at-start construction. Verdict: **correct on manual inspection** but the heuristic left it ambiguous because the call argument is computed across multiple lines.
- `R_non_fission_baseline` — uses the dry-at-end convention variant of `constant_thrust_burn` (the only round that does so). Manual inspection: the call sites in this round pass `m_v` (vehicle dry mass), `m_after_kick` (mass after chemical kick, equal to vehicle dry + chunk residual for the inbound burn), and `m_dry`. The dry-at-end convention is satisfied for all sites. Verdict: **correct on manual inspection**.

**Interpretation:**

The reactive triage already in the tree (`_audit`, `_revisit`, `_rerun` rounds) caught the contamination. The 3 in-scope contaminated rounds are exactly the three the campaign already knew about: hyperion's original (R_electric_outbound, fixed by R_electric_outbound_rerun), the megawatt cell sweep that titan-2 spot-checked, and the enceladus redundancy round whose outbound burn is incidental to its load-bearing cost finding.

The post-rerun, post-fix tree has 0 convention-incorrect rounds anchoring live verdicts. The reactive triage worked. The systematic audit confirms — does not extend — the catch list.

**Falsification check:**

- High (N > 12): no.
- Low (N < 3): no.

H1 held at N = 3.
