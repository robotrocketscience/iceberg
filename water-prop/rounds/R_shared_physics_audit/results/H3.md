# H3 — Tier-A verdict-flip

**Hypothesis (SCOPE):** ≥ 1 Tier-A re-run, when actually re-run with the corrected convention, flips a verdict currently being relied on for project-owner decision-making.

**Mimas point prediction:** H3 falsified (SCOPE's null) — the titan-3 closure cell is the only still-live closure, and it is anchored on clean rounds, not on contaminated rounds.

**Observed:** Tier-A count is 0 (per H2). With no Tier-A re-runs, H3's predicted ≥ 1 verdict-flip is vacuously impossible.

**Verdict:** **FALSIFIED in the SCOPE sense; HELD in the mimas-predicted sense.** No live-decision flip exists in the contamination set.

H3 was the SCOPE's null hypothesis — predicting at least one verdict-flip would make the audit *load-bearing* on a project-owner decision. The opposite outcome (zero flips) makes the audit *parallel cleanup*, which is the H6 load-bearing reading. The two are tightly coupled: H3 falsified ⇒ H6 held.

**Counter-check (sanity test against the titan-3 closure cell):**

If any of the 3 contaminated rounds anchored the 40-80 tonne / 30 kilowatt-electric closure cell, a re-run could in principle flip it. Verification by direct read of titan-3's round contributions:

- titan-3 R-chunk-size-pareto (commit `1997a51`) — the closure-cell-finding round. Its `run.py` does not import any of the 3 contaminated rounds' helper functions and does not redefine `constant_thrust_burn` from the contaminated formula. Its propellant accounting is its own clean computation.
- titan-3 R-delta-velocity-anchor-audit (commit `42120cf`) — corrected the Saturn-departure and Earth-arrival delta-velocities used by the closure cell; orthogonal to the function-convention bug class.
- titan-3 R-kilowatt-class-power-envelope (commit `5162735`) — 0 of 36 cells close at 1-30 kilowatt-electric × 200 tonnes; this is the round that constrains the still-live closure cell. No shared helper imports.

The titan-3 closure cell is anchored on clean rounds. The 3 contaminated rounds anchor cells that are now closed-by-directive or have been superseded by clean reruns.

**Falsification check (SCOPE language):**

- "Every Tier-A re-run reproduces its prior verdict within ±15%": vacuously true (zero Tier-A re-runs).

H3 falsified (zero flips) — which is the audit's preferred outcome and was mimas's predicted outcome.
